# ============================================
# API ConfigMap - Cermont FSM
# ============================================

apiVersion: v1
kind: ConfigMap
metadata:
  name: cermont-api-config
  namespace: cermont
data:
  frontend-url: "https://app.cermont.com"
  api-url: "https://api.cermont.com"
  log-level: "info"
  node-env: "production"
