# Runbook: Production Smoke Test Checklist- [ ] Health endpoint returns 200: `GET /api/health`- [ ] Login works: `POST /api/auth/login` with valid credentials- [ ] Dashboard loads: `GET /dashboard`- [ ] Can create work request: Navigate `/work-requests/new`- [ ] Orders list renders: `GET /orders`- [ ] Evidence upload works: `POST /api/evidences`- [ ] PWA loads offline: Check `/~offline` page- [ ] Forgot password sends email: `POST /api/auth/forgot-password`- [ ] RBAC restricts access: Test `/admin` as non-admin user- [ ] MongoDB connection OK: Check `/api/health/ready`- [ ] Static assets load: Check CSS, JS, images- [ ] Service worker registered: Check Application > Service Workers in DevTools## Commands```bash# Healthcurl -s https://<domain>/api/health# Logincurl -s -X POST https://<domain>/api/auth/login \  -H "Content-Type: application/json" \  -d '{"email":"gerencia@cermont.co","password":"Cermont2026!"}'# Check MongoDBcurl -s https://<domain>/api/health/ready```