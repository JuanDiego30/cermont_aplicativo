<!doctype html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Restablecer contraseña</title>
  </head>
  <body style="font-family: Arial, sans-serif; line-height: 1.6; color: #333;">
    <h2>Solicitud de restablecimiento de contraseña</h2>
    <p>Recibimos una solicitud para restablecer tu contraseña{{#if nombre}}, {{nombre}}{{/if}}.</p>
    {{#if resetUrl}}
      <p>
        Para continuar, abre este enlace: <a href="{{resetUrl}}">Restablecer contraseña</a>
      </p>
    {{/if}}
    {{#if expiresInMinutes}}
      <p>Este enlace expira en {{expiresInMinutes}} minutos.</p>
    {{/if}}
    <p style="font-size: 12px; color: #666;">Si no solicitaste esto, ignora este correo.</p>
  </body>
</html>
