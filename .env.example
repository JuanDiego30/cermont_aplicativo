# Configuración base para el backend Express + Next.js

# Conexión a Postgres (ajusta usuario/clave según tu entorno local)
DATABASE_URL=postgres://cermont_user:StrongPass@localhost:5432/cermontdb

# Secret para firmar JWT
JWT_SECRET=superSecretKeyChangeMe

# Origen permitido para CORS (puedes separar con comas varios orígenes)
FRONTEND_ORIGIN=http://localhost:3000

# Puerto del backend
PORT=4000

# Entorno de ejecución
NODE_ENV=development

# Opcional: nivel de logs del backend (info | debug)
LOG_LEVEL=info