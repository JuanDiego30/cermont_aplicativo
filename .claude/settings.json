{
	"permissions": {
		"allow": [
			"PowerShell(npm run typecheck 2>&1 | Select-Object -Last 12; \"EXIT: $LASTEXITCODE\")",
			"PowerShell(docker compose config --quiet 2>&1; \"CONFIG EXIT: $LASTEXITCODE\"; docker compose config --services 2>&1)",
			"PowerShell(npm run test 2>&1 | Select-Object -Last 40; \"EXIT: $LASTEXITCODE\")",
			"PowerShell(rg -v \"apiClient|lib/api|api-client|use-api\")",
			"PowerShell(npm -w @cermont/backend run test -- --run notification.service 2>&1 | Select-Object -Last 15; \"EXIT: $LASTEXITCODE\")"
		]
	}
}
