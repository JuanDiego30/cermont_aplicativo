# ================================================
# Cermont - Variables de Producción
# ================================================
# Copia este archivo a .env.production y configura los valores reales
# IMPORTANTE: Cambiar TODOS los valores por defecto antes de desplegar

# ---- Entorno ----
NODE_ENV=production

# ---- Dominio (CAMBIAR) ----
DOMAIN=cermont.tudominio.com
TRAEFIK_EMAIL=admin@tudominio.com

# ---- Traefik Auth (generar con: htpasswd -nb admin password) ----
TRAEFIK_BASIC_AUTH=admin:$$apr1$$xyz...

# ---- Base de Datos (CAMBIAR) ----
POSTGRES_USER=cermont_prod
POSTGRES_PASSWORD=CAMBIAR_POR_PASSWORD_SEGURO_32_CHARS
POSTGRES_DB=cermont_production

# ---- JWT (CAMBIAR - generar con: openssl rand -base64 32) ----
JWT_SECRET=CAMBIAR_POR_SECRET_SEGURO_MINIMO_32_CARACTERES
JWT_EXPIRES_IN=8h

# ---- Docker ----
TAG=latest
DOCKER_REGISTRY=

# ---- Opcional: Email ----
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
EMAILS_FROM=noreply@cermont.com

# ---- Opcional: Monitoreo ----
SENTRY_DSN=
