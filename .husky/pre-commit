#!/bin/sh
# Windows: MSYS2 fork() fails (STATUS_DLL_INIT_FAILED).
# Run the repository quality gates through one Node process so the hook works
# consistently in Windows MSYS2 and Unix shells.
exec node "./tooling/git/pre-commit.mjs"
