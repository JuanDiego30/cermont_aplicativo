#!/bin/sh
# Windows: MSYS2 fork() fails (STATUS_DLL_INIT_FAILED).
# exec node directly with full Windows path (translated to MSYS2 path).
# exec replaces sh with node via CreateProcess — no fork needed.
exec "/c/Program Files/nodejs/node.exe" "./node_modules/lint-staged/bin/lint-staged.js"
